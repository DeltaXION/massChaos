using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ApplicantItems : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //transform.SetParent(GameObject.Find("ApplicantList").transform);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
